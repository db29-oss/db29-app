[Unit]
Description=DB29 SCHEDULE
After=network.target

[Service]
User=root
WorkingDirectory=TO_BE_REPLACE
ExecStart=/usr/bin/php artisan schedule:run
Restart=always

[Install]
WantedBy=multi-user.target
